# Netlify configuration for the static site + Square booking functions.
[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

[functions]
  # Bundle the small shared lib (lib/square.mjs) alongside each function.
  node_bundler = "esbuild"
